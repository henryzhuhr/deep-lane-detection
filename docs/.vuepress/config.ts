import { defineUserConfig } from 'vuepress'

export default defineUserConfig({
    lang: 'zh-CN',
    title: 'Deep Lane Detection',
    description: 'Deep Lane Detection Documentation',
    base: "/deep-lane-detection/",
})